%% IRF
irf = impulseResponse;

figure();
axis = 1:400;
plot(axis, irf);

%% Conv
x = zeros(1, 400);
x(100) = 1;

y = conv(x, irf, 'same');

subplot(1, 2, 1);
plot(axis, x);

subplot(1, 2, 2);
plot(y);